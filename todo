done:
* Virtualenv cleaning
* Virtualenv re-use
* Ignoring exit codes
* Running functions instead of just commands
* Create setup.py and such (turn it into a package)
* Write tests
* sdist
* whitelist_externals

todo:
* Update README.rst
* fail() and succeed()
* Setuptools integration (https://tox.readthedocs.org/en/latest/example/basic.html#integration-with-setuptools-distribute-test-commands)
